/* #include <stdio.h>

float num1, num2, result = 0;
int choice = 0;

int main(void) {
  printf("Enter num1: ");
  scanf("%f", &num1);
  printf("Enter num2: ");
  scanf("%f", &num2);
  printf("Menu\n1 - Add\n2 - Subract\n3 - Divide\n4 - Multiply ");
  printf("Enter choice: ");
  scanf("%d", &choice);

  switch (choice) {
  case 1:
    result = num1 + num2;
    break;
  case 2:
    result = num1 - num2;
    break;
  case 3:
    result = num1 * num2;
    break;
  case 4:
    result = num1 / num2;
    break;
  }

  printf("Result is: %.2f", result);

  return 0;
} */